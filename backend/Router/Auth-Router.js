const express = require("express");
const router = express.Router();

// basic sntax for deifining path .. We can skip route
// router.route.get("/regi", (req, res) => {
//   res.status(200).send("Welcome to the home page!");
// });

// module.exports = router;
